/**
 * Centralized SEO configuration.
 *
 * Single source of truth for the site's public URL and brand used across
 * metadata, canonical/hreflang generation, and structured data.
 */

export const SITE_URL = "https://feizfood.com";
export const SITE_NAME = "Feiz Food Group";
export const SITE_DOMAIN = "feizfood.com";

/** Default Open Graph image (existing hero asset — not the logo). */
export const DEFAULT_OG_IMAGE = "/media/hero/cargo-ship-wide.jpg";
